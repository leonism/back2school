/** @type {import('tailwindcss').Config} */
module.exports = {
  // Configuration has been moved to src/input.css
  // This file is kept for compatibility or can be deleted if not needed.
  content: ['./dist/**/*.html'], // Keep content for now, v4 might still pick it up or ignore safely
  plugins: [],
};
